class StoryConstants {
  static const Duration defaultStoryDuration = Duration(seconds: 5);
  static const Duration storyTransitionDuration = Duration(milliseconds: 250);
  static const Duration storyGroupTransitionDuration =
      Duration(milliseconds: 250);
}
